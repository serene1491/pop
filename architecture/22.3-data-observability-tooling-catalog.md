# Data, Observability, and Tooling Catalog

## Databases and storage

| Root | Tier/status/phase | Scope and public concepts | Dependencies, portability, cost, security, extension, exclusions |
| --- | --- | --- | --- |
| `Data` | official; bootstrap Package, APIs planned; phase 5 | typed rows, columns, schemas, datasets, tabular frames, query values, nullability, bulk transforms; `Data.Row`, `Column`, `Schema`, `Set`, `Frame`, `Query` | Codec/Sequence/Task; pure typed values first; column/view buffers avoid copies; schemas resolved/generated; no universal dynamic row or property bag; owned by separately installed `Pop.Data` |
| `Sql` | official; planned; phase 5 | typed `Row`, `Column`, `Schema`, `Query`, parameters, connection/pool/cursor/transaction/savepoint; `open`, `prepare`, `query`, `execute`, `transaction`; `Sql.Migrate`, `Test` | Codec/Task/Time/Telemetry + explicit driver Package; rows stream and buffers may be reused; parameters never interpolated; cancellation/timeouts required; driver is typed function record, not provider hierarchy |
| `Store` | official/platform; planned; phase 5 | key-value, document, object/blob, embedded storage contracts; `Store.Key`, `Value`, `Batch`, `Cursor`; `Store.Kv`, `Document`, `Blob`, `Embedded`, `Cloud` | Codec/Io/Task/Telemetry + explicit engine Package; streaming values and batch costs documented; key/schema/transaction limits; no universal dynamic document type except explicit `Json.Value` |

Tabular data uses typed records and schemas. Data-frame/tensor analysis belongs
to `Science.Frame` and `Tensor`, not the persistence layer. Database nullability
maps to explicit optional fields. Bulk operations, isolation, retries, and
telemetry are typed option records. Transactions are resource scopes owned by
`Sql` or the relevant `Store` engine, never a top-level namespace.

## Telemetry

| Root | Tier/status/phase | Scope and public concepts | Dependencies, portability, cost, security, extension, exclusions |
| --- | --- | --- | --- |
| `Telemetry` | standard/official; planned; phase 5 | typed `Event`, `Field`, `Level`, correlation/scope; `Telemetry.Log`, `Trace`, `Metric`, `Crash`, `Test`; `log`, `span`, `count`, `measure` | Time/Task/Source; standard contracts + no-op/test sinks, official exporters; disabled paths have measurable near-zero budgets; buffering/sampling explicit; redaction required; no mandatory global singleton or string field bags |

`Telemetry.Log` defines structured events and filters; `Trace` defines spans,
links, propagation, and sampling; `Metric` defines counters, gauges, histograms,
units, and aggregation; `Crash` defines stack/source reports. Sinks/exporters are
explicit typed values or compile-time registered generated adapters. A scoped
application default may be passed at entry points, but APIs also accept an
explicit `Telemetry.Sink` and tests can capture it deterministically.

## Commands and settings

| Root | Tier/status/phase | Scope and public concepts | Dependencies, portability, cost, security, extension, exclusions |
| --- | --- | --- | --- |
| `Cli` | official/tooling; bootstrap Package, APIs planned; phase 5 | application entry composition, typed command dispatch, exit/diagnostic mapping, terminal integration; `Cli.App`, `Run`, `Error`, `Test` | Command/Settings/Terminal/Process; generated dispatch and direct functions; no service container, reflection scan, or hidden process authority; owned by separately installed `Pop.Cli` |
| `Command` | official/tooling; planned; phase 5 | typed command/option/argument metadata, parser, help/completion/man pages, exit/diagnostic mapping; `Command.App`, `Parse`, `Help`, `Complete` | Metadata/Locale/Terminal/Process; generated parser preferred; bounded inputs; secret options redacted; no reflection scan or service dispatch |
| `Settings` | official; planned; phase 5 | immutable typed settings from defaults/files/environment/arguments/secrets; `Settings.Source`, `Merge`, `Validate`, `Watch` | Codec/Environment/File/Locale/Metadata; generated typed loaders; precedence explicit; reload produces new value; secret/trust/path boundaries required; no untyped configuration map or builder chain |

Dependency injection is not a library container. Applications compose immutable
configuration, capability values, function records, and real resource handles
directly. Compile-time metadata may generate wiring functions, but resolution is
typed and visible in generated code.

## Testing and benchmarks

| Root | Tier/status/phase | Scope and public concepts | Dependencies, portability, cost, security, extension, exclusions |
| --- | --- | --- | --- |
| `Test` | tooling; planned; phase 1 onward | assertions, parameter cases, property tests, snapshots/golden files, fuzz inputs, test clocks/files/transports/randomness, capture, fixtures; `Test.Property`, `Snapshot`, `Fuzz`, `Fake` | Standard + test runner; deterministic seed/time by default; failure values structured; temporary resources scoped; production Bubbles do not depend on it |
| `Benchmark` | tooling; planned; phase 1 onward | benchmark cases, warmup/sampling, allocation/native-transition counters, baselines, comparison and machine facts | Time/Telemetry/Platform; harness overhead measured; statistical output structured; no claims from one run or incomparable target |

Mocks are not a default abstraction. Tests pass explicit function/capability
records or real in-memory implementations. Snapshot updates require deliberate
review and preserve structured differences.

## Compiler and package tooling APIs

| Root | Tier/status/phase | Scope and public concepts | Dependencies, portability, cost, security, extension, exclusions |
| --- | --- | --- | --- |
| `Metadata` | standard/tooling; planned; phase 2/6 | nominal attribute values, target/use schemas, generated adapter identities, opt-in retained projections; `Metadata.Use`, `Schema`, `Generate`, `Retain` | compiler UDA contract/Source/Diagnostic; compile-time access is typed and budgeted; retained data is explicit and serializable; no runtime enumeration/get-by-name, reflection registry, or compiler handle escape |
| `Source` | official/tooling; bootstrap Package, APIs planned; phase 6 | immutable source text, line map, span, edit, workspace edit; `Source.Text`, `Span`, `Edit` | compiler public schemas; incremental views avoid copies; encoding fixed; no compiler arenas/IDs leak as runtime reflection; owned by separately installed `Pop.Syntax` |
| `Syntax` | official/tooling; bootstrap Package, APIs planned; phase 6 | versioned token kinds, trivia, immutable lossless nodes/trees, parse results, visitors, typed edits and serialization; `Syntax.Token`, `Trivia`, `Node`, `Tree`, `Parse`, `Edit` | Source/Diagnostic; public facade designed independently of private Rust parser structs; bounded parse/recovery; incremental views avoid copies; owned by separately installed `Pop.Syntax` |
| `Lsp` | official/tooling; bootstrap Package, APIs planned; phase 6 | protocol versions, client/server capabilities, requests, notifications, diagnostics transport, semantic tokens, completion, hover, workspace edits, progress/cancellation and test transports | Pop.Syntax + Pop.Rpc + standard Json/Task; bounded untrusted messages; generated typed protocol identities; no compiler/query handles or runtime method-name dispatch; owned by separately installed `Pop.Lsp` |
| `Diagnostic` | tooling; planned; phase 6 | stable code, severity/category, typed arguments, labels/notes/origin, fix, JSON/SARIF rendering | Source/Locale; structured values shared by CLI/LSP; rendering allocates separately; no message parsing to recover facts |
| `Package` | tooling; planned; phase 6 | manifest/lock/identity/version/dependency/target metadata, registry/package operations; `Package.Manifest`, `Lock`, `Graph`, `Registry` | Version/Uri/Crypto/Http/Source; deterministic resolution and signed/hash-checked artifacts; credentials explicit; no runtime package loading by string |
| `Documentation` | tooling; planned; phase 6 | checked XML docs, semantic references, examples, indexes/search artifacts; `Documentation.Xml`, `Model`, `Render`, `Test` | Source/Diagnostic/Package; safe XML parser; incremental generated index; documentation metadata never enables runtime reflection |

Source and syntax-tree APIs are owned by `Pop.Syntax`; diagnostics may remain a
separate tooling Package. Semantic queries, resolved AST, HIR, MIR, and compiler
incremental databases are excluded until separately designed. Public facades
cannot expose private compiler handles or permit source injection through
compile-time execution.

## Typed metadata lifecycle

Attributes are language syntax whose values are nominal library-defined types.
The compiler knows only reserved roles such as `@CompileTime`, usage/validator
contracts, and explicitly accepted code-generation/retention roles. Domain
Packages define attributes such as `@Command`, `@Description`, `@Category`,
`@Parameter`, `@Deprecated`, `@Stable`, `@RequiresCapability`, `@Serializable`,
`@Validate`, `@Route`, `@Message`, `@Generated`, and `@Test`.

Metadata values live in compiler-owned typed schemas during compilation.
Processors run under the deterministic compile-time budget/capability contract
and may emit structured diagnostics, adapter schemas, or generated bindings
through approved tooling APIs. Invalid target, duplicate, version, or
cross-attribute combinations are diagnosed by resolved identity. Runtime
retention is opt-in, versioned, visibility-preserving, and limited to generated
serializable projections. There is no symbol enumeration, reflection scan, or
get/call-by-name API.

`Package.Plugin` describes typed build/tool extensions in manifests. Plugins run
out of process or under the compile-time capability sandbox, declare inputs and
outputs, and exchange versioned schemas. A plugin cannot change grammar,
tokenization, visibility, or inject source text.
