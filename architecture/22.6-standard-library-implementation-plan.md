# Public Library Implementation and Migration Plan

## Delivery rule

Architecture placement does not stabilize an API. Each implementation slice
follows architecture, failing conformance tests, minimal implementation, API and
cost review, then documentation. A phase may overlap in research, but its public
surface cannot stabilize before its prerequisites and definition of done.

## Phase plan

| Phase | Families and prerequisites | Deliverables and dependency order | Required gates and definition of done |
| --- | --- | --- | --- |
| 0. Baselines | `Pop.Standard`, prelude, architecture tests; stable IDs, `Result`, docs metadata, Bubble artifacts | exact root/tier manifest; prelude/API snapshot; status metadata; docs cost/effect schema | reject duplicate/missing roots, upward dependencies, stale names, dynamic/OOP patterns; all docs checked; no planned API emitted as implemented |
| 1. Core values | `Math`, `Random`, `Guid`, `Version`, `Uri`, `Mime`, `Sequence`, `Text`, `Unicode`, `Bytes`; generics, views/lifetimes, iteration, checked numeric semantics | values before algorithms; Unicode/Bytes before Text; Text/Sequence before codecs | property/fuzz/differential tests; UTF-8 and numeric boundaries; allocation/copy fixtures; complexity benchmarks; API call-site review; portable on interpreter and LLVM |
| 2. Formats and locale | `Codec`, `Json`, `Yaml`, `Xml`, `Csv`, `Toml`, `Regex`, `Glob`, `Locale`, `Resource`, `Metadata`, `Time`; typed UDA processors and generated adapters | Metadata identities and Codec schema first; streaming primitives; formats; locale/time data; YAML resource compiler | malformed/limit/canonicalization tests; entity/alias/depth bombs; locale fixtures; no reflection/string keys; throughput/allocation baselines; generated documentation and examples |
| 3. I/O and host | `Io`, `Path`, `Memory` safe views, `File`, `Directory`, `Process`, `Environment`, `Platform`, `Terminal`, `Archive`, `Compress`; PLRI handles, cleanup, target capabilities | portable values/protocols before PLRI adapters; file/directory before archive; standard terminal before optional UI | fault injection, cleanup/cancellation, traversal/symlink/permission tests; target matrices; syscall/native-transition benchmarks; sandbox/capability review; unsupported behavior typed |
| 4. Structured concurrency and network | `Task`, `Channel`, `Atomic`, `Net`, `Socket`, `Crypto`, `Http`, `WebSocket`; accepted async/cleanup semantics, scheduler, TLS | task/cancel/deadline before transports; crypto/cert before TLS/HTTP; sockets before protocol Packages | race/leak/cancellation/backpressure tests; deterministic scheduler; protocol differential/fuzz tests; certificate/redirect/header/body limits; pool/stream benchmarks; cross-backend task semantics |
| 5. Data and operations | `Pop.Data` (`Data`, `Sql`, `Store`), `Telemetry`, `Pop.Cli` (`Cli`, `Command`, `Settings`), `Schedule`; driver/function-record and metadata generation contracts | extension manifests first; telemetry primitives before integrations; typed data/SQL/store adapters; CLI/settings/schedule adapters | independent extension builds; injection/schema/transaction/rollback tests; driver contract suites; redaction and deterministic capture; query/log/job overhead benchmarks; migration and generated-source review |
| 6. Tooling, RPC, and interop | `Pop.Syntax`, `Pop.Rpc`, `Pop.Lsp`, `Test`, `Benchmark`, `Diagnostic`, `Package`, `Documentation`, `Ffi`; stable public source/syntax/protocol/artifact schemas and ABI metadata | Syntax facade before LSP; RPC before LSP; private compiler adapters after public schemas; FFI generator after target/ABI model | independent extension builds; schema/protocol compatibility and reproducibility tests; bounded malformed-input tests; unsafe negative tests; ABI/callback/GC-root tests; no compiler-private handle leaks |
| 7. Rich applications and science | `Image`, `Graphics`, `Audio`, `Video`, `Media`, `Ui`, `Geometry`, `Units`, `Statistics`, `Tensor`, `Signal`, `Science`; resource-safe native/GPU/device adapters | values/codecs before render/play; Tensor before higher science; headless UI before platform renderers | corpus/fuzz/license checks; frame/buffer lifetime tests; accessibility/headless snapshots; numeric tolerances; CPU/GPU transfer/allocation benchmarks; platform feature matrices |
| 8. Distributed and specialist ecosystems | `Message`, `Email`, `Identity`, `Pop.Ai`, `Device`; preceding network/security/Pop.Data/media/science contracts | data contracts before AI; schemas and identity before remote AI/tools; device capabilities before capture; broker/vendor adapters separate | independent AI build; protocol/auth/safety/credential tests; deterministic in-memory transports; model/runtime hashes and reproducibility; device permission matrices; vendor-neutral conformance |

## Review gates per API family

Each family proposal includes:

1. Package/Bubble identity, public roots/subnamespaces, tier, dependencies, and
   target capability manifest.
2. Three call sites: concise default, advanced configuration, and efficient
   stream/view/buffer/resource use.
3. Complete public types/functions/errors with no placeholder dynamic values.
4. Allocation, ownership, copying, iteration, dispatch, blocking/suspension,
   native-boundary, and asymptotic-cost table.
5. Positive, rejection, limits, cleanup, cancellation, portability, security,
   and cross-backend test matrix.
6. Benchmarks that measure any proposed performance promise; intended but
   unmeasured goals remain labeled as such.
7. Checked XML documentation, compiled examples, migration notes, and an API
   baseline classified as experimental or stable.

## First implementation pull requests

These slices establish reusable conventions without pretending to deliver the
whole ecosystem:

1. **Publish the library manifest schema.** Add tier/status/capability/cost
   metadata and machine checks for the root inventory. No new runtime API.
2. **Bootstrap the official extension Packages.** Add independent manifests,
   versions, dependency graphs, namespace roots, and builds for `Pop.Data`,
   `Pop.Ai`, `Pop.Cli`, `Pop.Rpc`, `Pop.Syntax`, and `Pop.Lsp`.
3. **Freeze the bootstrap prelude baseline.** Inventory `print`, primitive and
   protocol identities; decide which roots, if any, are implicitly available.
4. **Establish `Bytes` views and reusable buffers.** Define ownership, bounds,
   growth, allocation fixtures, HIR/MIR representation, and interpreter/LLVM
   differential tests.
5. **Establish `Text.View` and Unicode-safe slicing.** Build on Bytes, pin the
   Unicode data version, and replace ambiguous bootstrap slicing contracts.
6. **Establish lazy `Sequence` adapters.** Prove no hidden materialization,
   short-circuit behavior, and exact iterator protocol identities.
7. **Establish `Codec` adapter identities plus bounded `Json.decode`.** Use a
   small typed record schema and direct decode path; defer general code
   generation until the metadata proposal is complete.
8. **Establish `Path` lexical values.** Keep filesystem authority out of the
   first slice; test target-independent normalization and native encoding facts.
9. **Establish `Time.Duration` and injected `Clock`.** Separate pure values from
   PLRI clocks and create deterministic test-time conventions.

Each is independently reviewable and leaves the workspace passing. `File`,
async tasks, network, and cryptography follow only after the relevant resource,
effect, and PLRI decisions are accepted.

## Current implementation inventory and migration

| Current surface | Status | Required action |
| --- | --- | --- |
| `print` bootstrap function | implemented bootstrap | retain provisionally; specify terminal/stream, encoding, allocation, and error behavior before stabilization |
| checked arithmetic helpers | prototype | map into `Math`; keep exact overflow semantics and add API/cost baseline |
| text slicing helpers | prototype | replace byte ambiguity with explicit `Text.View`/byte operations; migration diagnostic if spelling changes |
| sequence helpers | prototype | retain only after lazy/materializing behavior and allocation are tested |
| prelude protocols/types | implemented bootstrap metadata | snapshot exact identities; additions require compatibility and collision review |
| trusted attributes | implemented bootstrap metadata | keep compiler roles; public metadata families require separate generated-adapter work |

There is no released broad standard API to preserve. Compatibility is not
claimed for planned names. Once a public API is stabilized, renames require a
deprecation edition, exact semantic replacement, optional automated rewrite,
and documentation migration. An alias is rejected when it widens the prelude,
keeps a verbose model alive, or obscures a cost/security change.

## Documentation ownership

The owning Package maintains its catalog row, checked API documentation,
examples, cost table, capability/security notes, target matrix, API snapshot,
benchmarks, and migration guide. The architecture index owns root placement and
tier rules. Tooling renders status prominently so planned and implemented APIs
cannot be confused.

## Unresolved dependencies

- Accepted syntax and MIR semantics for result propagation, async functions,
  awaiting, and scoped cleanup.
- Borrow/view lifetime rules sufficient for non-owning text, bytes, frames, and
  native buffers.
- Generic specialization and code-size policy.
- Public effect/cost metadata schema and which facts are compiler-checked.
- Stable public compiler/source schemas for tooling Packages.
- Initial supported operating systems, architectures, and WebAssembly/web-host
  capability profile.
- Official Package signing, codec licensing metadata, and security response
  policy.
- Exact prelude root list; no catalog root is implicitly imported merely by
  appearing in this architecture.
