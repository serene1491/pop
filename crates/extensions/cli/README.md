# Pop.Cli

Bootstrap Package for optional official command-line application APIs. It owns
`Pop.Cli`, `Pop.Command`, and `Pop.Settings`, remains outside `Pop.Standard`,
and does not replace low-level standard terminal contracts.
