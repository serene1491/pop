# Pop.Data

Bootstrap Package for the optional official data ecosystem. It owns the
`Pop.Data`, `Pop.Sql`, and `Pop.Store` namespaces and is not part of
`Pop.Standard`. Public APIs remain planned until their focused conformance work.
