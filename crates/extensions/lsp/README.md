# Pop.Lsp

Bootstrap Package for official LSP and editor protocol contracts. It depends on
`Pop.Rpc` and `Pop.Syntax`, remains outside `Pop.Standard`, and will later form
the public protocol boundary of official tooling without exposing compiler
internals.
